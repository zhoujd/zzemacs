version: "3"

services:
  emacs:
    container_name: zzemacs
    hostname: zzemacs
    image: "ubuntu-2204-zzemacs:zach"
    network_mode: bridge
    extra_hosts:
      - "myhost:${MYHOST}"
    restart: always
    ports:
      - "10022:22"
    privileged: true
    cap_add:
      - ALL
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - /home/${USER}/zzemacs:/home/${USER}/zzemacs
      - /home/${USER}/work:/home/${USER}/work
