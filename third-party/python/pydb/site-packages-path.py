#!/usr/bin/python
#$Id: site-packages-path.py,v 1.1 2006/01/11 04:10:32 rockyb Exp $
"""Find where site-path is located.
We print the first location and exit with code 0.
If we don't find the path, exit with code 1.
"""
import sys, os.path
for dir in sys.path:
    short_dir=os.path.basename(dir)
    ##suse for site-package, ubuntu python2.7 for dist-packages
    if short_dir == 'site-packages' or short_dir == 'dist-packages':
        print dir
        sys.exit(0)
#Not found
sys.exit(1)
