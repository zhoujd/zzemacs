#!/bin/sh

case $1 in
    -v|--version|-h|--help )
        exec tig "$@"
        ;;
    * )
        repo=$(basename `git rev-parse --show-toplevel`)
        exec st -t "tig:$repo $$@$(hostname)" -e tig "$@" >/dev/null 2>&1
        ;;
esac
