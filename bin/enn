#!/bin/sh

INS=nnn
CMD="nnn"
TITLE="nnn $$@$(hostname)"

case $1 in
    -h|--help|-V )
        exec nnn "$@"
        ;;
    * )
        exec st -t "$TITLE" -n "$INS" -e "$CMD" "-e" 2>/dev/null
        ;;
esac
