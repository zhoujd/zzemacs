## zzemacs daemon

services:
  emacs-daemon:
    hostname: ubuntu-2204-daemon
    image: zhoujd/ubuntu-22.04-daemon:dev
    container_name: emacs-daemon
    network_mode: bridge
    extra_hosts:
      - "myhost:host-gateway"
    env_file: .env
    restart: always
    command:
      - "run"
    privileged: true
    cap_add:
      - ALL
    volumes:
      - /tmp:/tmp
      - /var/run/docker.sock:/var/run/docker.sock
      - ${HOME}/.ssh:/home/${USER}/.ssh
      - ${HOME}/.emacs:/home/${USER}/.emacs
      - ${HOME}/.fonts:/home/${USER}/.fonts
      - ${HOME}/zzemacs:/home/${USER}/zzemacs
