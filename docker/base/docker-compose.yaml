version: "3"

services:
  zzemacs:
    hostname: ubuntu-2004-zzemacs
    image: ubuntu-2004-zzemacs:base
    container_name: zzemacs-base
    network_mode: bridge
    extra_hosts:
      - "myhost:host-gateway"
    restart: always
    ports:
      - 10022:22
    privileged: true
    cap_add:
      - ALL
    volumes:
      - /dev:/dev
      - /var/run/docker.sock:/var/run/docker.sock
      - /etc/security/limits.conf:/etc/security/limits.conf
      - ${ZZEMACS_ROOT}:/home/${USER}/zzemacs
      - ${ZZEMACS_TOP}/lab:/home/${USER}/lab
